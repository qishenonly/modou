/**
 * 审批弹窗（T-044）：展示待执行操作详情（命令全文/diff），三选项裁决
 * （allow_once / allow_always / deny），裁决经 `approve` Command 回传 core。
 *
 * T-040 骨架忽略 approval_request / approval_resolved 事件（且缺省只读工具，
 * 不会产生审批请求）；本文件留空壳，T-044 接入弹窗交互。
 */
export {};
